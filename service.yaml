apiVersion: v1
kind: Service
metadata:
  name: trend-service
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "80"
spec:
  type: LoadBalancer
  selector:
    app: trend-app
  ports:
    - port: 80
      targetPort: 80
